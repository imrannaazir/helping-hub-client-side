import React from 'react';
import Header from '../Header/Header';

const Events = () => {
    return (
        <div>
            <Header />
            events
        </div>
    );
};

export default Events;